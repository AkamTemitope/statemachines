import { Options } from '../types';
interface CliOptions extends Options {
    /**
     * Folder for output files. If none output will be written to stdout.
     */
    output?: string;
    help?: boolean;
    debug?: boolean;
}
export declare function cliMain(o: CliOptions): Promise<void>;
export {};
